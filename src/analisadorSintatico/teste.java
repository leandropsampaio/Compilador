/*
private void variavelConstanteObjeto() {

        if (validarToken("var")) {
            if (validarToken("{")) {
                tipo();
                tratamentoIdentificador();
                if (validarToken(";")) {
                    variavelConstanteObjeto();
                } else {
                    //panicMode();
                }
                variavelConstanteObjeto();
            }
        } else if (validarToken("Identificador")) {
            if (validarToken("Identificador")) {
                criarObjetos();
            } else if (validarToken("=")) {
                instancia();
            } else {
                chamadaMetodo();
            }
            variavelConstanteObjeto();
        } else if (validarToken("float") || validarToken("int") || validarToken("string") || validarToken("bool")) {
            tipoVazio();
            tratamentoVariavel();
        } else {
        }

    }

private void tratamentoIdentificador() {
        if (validarToken("Identificador")) {
            if (validarToken("=")) {
                verificarExpressao();
                geradorConstante();
            }
            geradorConstante();
        } else {
            panicMode();
        }
    }












*/
